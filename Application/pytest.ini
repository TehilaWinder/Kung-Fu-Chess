[pytest]
; מוסיף את תיקיית השורש (איפה שנמצא config.py) ל-sys.path,
; כדי ש-"import config" ו-"from Entities... import ..." יעבדו
; בלי קשר לאיך שמריצים את pytest (מהשורש, מ-VSCode, וכו').
pythonpath = .
testpaths = Tests
